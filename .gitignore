# Local secrets and generated output. update-netbox.json embeds the values from .env.
.env
workflow/update-netbox.json
workflow/update-netbox.remote.json

# pfSense configs (even redacted ones)
pfsense/
*.xml
scan-secrets.sh
# Local tooling
scan-secrets.sh
